spring:
  datasource:
    url: jdbc:mysql://localhost:3306/viraj
    username: root
    password: vivek02
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
